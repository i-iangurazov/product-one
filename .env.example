# Database
DATABASE_URL="postgresql://neondb_owner:npg_bhlLgYrMP16X@ep-small-fire-a4vz5c4g-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require"

# API auth (set your own secrets in production)
API_JWT_SECRET="dev-secret"
API_JWT_SECRET_STAFF="dev-secret-staff"
API_JWT_SECRET_PLATFORM="dev-secret-platform"
STAFF_TOKEN_TTL_SECONDS=86400
PLATFORM_TOKEN_TTL_SECONDS=86400
STAFF_REFRESH_TOKEN_TTL_DAYS=14

# Refresh cookie scope
STAFF_REFRESH_COOKIE_NAME="qr_staff_r"
STAFF_REFRESH_COOKIE_PATH="/auth"
# lax | strict | none (none requires HTTPS)
STAFF_REFRESH_COOKIE_SAMESITE="lax"
# Optional explicit domain (defaults to FRONTEND_BASE_URL host)
# STAFF_REFRESH_COOKIE_DOMAIN="localhost"

# Frontend + CORS
FRONTEND_BASE_URL="https://product-one-web.vercel.app"
CORS_ALLOWED_ORIGINS="https://product-one-web.vercel.app"
CORS_ALLOW_LOCALHOST=true

# Owner login seed (change in prod)
PLATFORM_OWNER_EMAIL="owner@example.com"
PLATFORM_OWNER_PASSWORD="Owner123!"

# Auth rate limit (per key)
AUTH_RATE_LIMIT_MAX=20
AUTH_RATE_LIMIT_WINDOW_MS=60000

# Dangerous scripts
# Set to true to allow running purge-orders script in dev/test
CONFIRM_PURGE_ORDERS=false

# Refresh cookie config
REFRESH_COOKIE_NAME="qr_staff_r"
REFRESH_COOKIE_PATH="/auth"
REFRESH_COOKIE_DOMAIN=""
REFRESH_COOKIE_SAMESITE="lax" # lax | strict | none
REFRESH_COOKIE_SECURE=true
REFRESH_COOKIE_MAX_AGE_DAYS=14

# Demo staff password for seed script
STAFF_DEMO_PASSWORD="ChangeMe123!"

# Optional session tuning
SESSION_INACTIVITY_MS=5400000
CLOSED_SESSION_TTL_MS=86400000
SERVED_ORDER_TTL_MS=86400000

# Next.js client API targets
NEXT_PUBLIC_API_HTTP="https://qrapi-production.up.railway.app"
NEXT_PUBLIC_API_WS="https://qrapi-production.up.railway.app"
